services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: amsadmin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set (see .env)}
      POSTGRES_DB: amsdb
    ports:
      - "5432:5432"
    volumes:
      - ams-postgres-data:/var/lib/postgresql/data

volumes:
  ams-postgres-data:
